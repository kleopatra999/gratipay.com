"""Show information about a single Team. It might be your team!
"""
from collections import OrderedDict

from aspen import Response
from gratipay.utils import markdown, truncate

[-----------------------------------------------------------------------------]

team = request.path['team']
if team.package:
    website.redirect(team.package.url_path)
banner = name = team.name
suppress_sidebar, is_team_owner = team.get_ui_state(user)

[-----------------------------------------------------------------------------]
{% include "templates/project.html" %}
